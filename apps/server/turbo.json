{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "dev": { "dependsOn": ["@envhandoff/web#build"] },
    "test": { "dependsOn": ["^build", "@envhandoff/web#build"] }
  }
}
